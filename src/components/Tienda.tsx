import React from "react";
import { Buttons, Container } from "../styledComponets/Tienda";
import { Modal,  TextField , Button } from "@mui/material";

function Tienda() {
  return (
    <Container>
        <h1> Tienda </h1>
        <Buttons > Añadir </Buttons>

    </Container>
    )
}

export default Tienda;